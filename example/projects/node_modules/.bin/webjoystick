#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../webjoystick/bin/webjoystick" "$@"
  ret=$?
else 
  node  "$basedir/../webjoystick/bin/webjoystick" "$@"
  ret=$?
fi
exit $ret
